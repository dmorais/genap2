<!-- Use the file tool_data_table_conf.xml.oldlocstyle if you don't want to update your loc files as changed in revision 4550:535d276c92bc-->
<tables>

    <!-- Locations of indexes in the Bowtie color-space mapper format -->
     <table name="bowtie_indexes_color" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/bowtie_indices_color.loc" />
        </table>
    <!-- Locations of indexes in the Bowtie mapper format -->
    <table name="bowtie_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/bowtie_indices.loc" />
    </table>
    <!-- Locations of indexes in the Bowtie mapper format -->
    <table name="tophat_indexes" comment_char="#">
         <columns>value, dbkey, name, path</columns>
         <file path="tool-data/bowtie_indices.loc" />
     </table>
    <!-- Locations of 2bit sequence files for use in Lastz -->
    <table name="lastz_seqs" comment_char="#">
        <columns>value, name, path</columns>
        <file path="tool-data/lastz_seqs.loc" />
    </table>
    <table name="bfast_indexes" comment_char="#">
        <columns>value, dbkey, formats, name, path</columns>
        <file path="tool-data/bfast_indexes.loc" />
    </table>
    <table name="sam_fa_indexes" comment_char="#">
        <columns>line_type, value, path</columns>
        <file path="tool-data/sam_fa_indices.loc" />
     </table>
    <!-- Locations of all fasta files under genome directory -->
    <table name="all_fasta" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/all_fasta.loc" />
    </table>
    <!-- Locations of indexes in the BFAST mapper format -->
    <table name="bfast_indexes" comment_char="#">
        <columns>value, dbkey, formats, name, path</columns>
        <file path="tool-data/bfast_indexes.loc" />
    </table>
    <!-- Locations of nucleotide (mega)blast databases -->
    <table name="blastdb" comment_char="#">
        <columns>value, name, path</columns>
        <file path="tool-data/blastdb.loc" />
    </table>
    <!-- Locations of protein (mega)blast databases -->
    <table name="blastdb_p" comment_char="#">
        <columns>value, name, path</columns>
        <file path="tool-data/blastdb_p.loc" />
    </table>
    <!-- Locations of indexes in the BWA mapper format -->
    <table name="bwa_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/bwa_index.loc" />
    </table>
    <!-- Locations of indexes in the BWA color-space mapper format -->
    <table name="bwa_indexes_color" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/bwa_index_color.loc" />
    </table>
    <!-- Locations of MAF files that have been indexed with bx-python -->
    <table name="indexed_maf_files">
        <columns>name, value, dbkey, species</columns>
        <file path="tool-data/maf_index.loc" />
    </table>
    <!-- Locations of fasta files appropriate for NGS simulation -->
    <table name="ngs_sim_fasta" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/ngs_sim_fasta.loc" />
    </table>
    <!-- Locations of PerM base index files -->
    <table name="perm_base_indexes" comment_char="#">
        <columns>value, name, path</columns>
        <file path="tool-data/perm_base_index.loc" />
    </table>
    <!-- Locations of PerM color-space index files -->
    <table name="perm_color_indexes" comment_char="#">
        <columns>value, name, path</columns>
        <file path="tool-data/perm_color_index.loc" />
    </table>
    <!-- Location of Picard dict file and other files -->
<table name="picard_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/picard_index.loc" />
    </table>
    <!-- Location of SRMA dict file and other files -->
    <table name="srma_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/picard_index.loc" />
    </table>
    <!-- Location of Mosaik files -->
    <table name="mosaik_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/mosaik_index.loc" />
    </table>
    <table name="gatk_picard_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/gatk_sorted_picard_index.loc" />
    </table>
     <!-- Available of GATK references -->
    <table name="gatk_annotations" comment_char="#">
         <columns>value, name, gatk_value, tools_valid_for</columns>
          <file path="tool-data/gatk_annotations.txt" />
     </table>
         <!-- Locations of configurations in the CCAT peak/region caller format -->
    <table name="ccat_configurations" comment_char="#">
        <columns>value, name, path</columns>
         <file path="tool-data/ccat_configurations.loc" />
     </table>
     <!-- Location of Picard dict files valid for GATK -->
    <table name="gatk_picard_indexes" comment_char="#">
       <columns>value, dbkey, name, path</columns>
       <file path="tool-data/gatk_sorted_picard_index.loc" />
    </table>
    <!-- Locations of add_scores files under genome directory -->
    <table name="add_scores" comment_char="#">
       <columns>value, path</columns>
        <file path="tool-data/add_scores.loc" />
     </table>
     <!-- Locations of alignseq files under genome directory -->
<table name="alignseq" comment_char="#">
       <columns>line_type, value, name, path</columns>
       <file path="tool-data/alignseq.loc" />
    </table>
    <!-- Locations of binned_scores files under genome directory -->
    <table name="binned_scores" comment_char="#">
         <columns>value, description, path</columns>
         <file path="tool-data/binned_scores.loc" />
     </table>
         <!-- Locations of bowtie2_indices files under genome directory -->
    <table name="bowtie2_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/bowtie2_indices.loc" />
    </table>

   <!-- Locations of tophat2_indices files under genome directory -->
    <table name="tophat2_indexes" comment_char="#">
         <columns>value, dbkey, name, path</columns>
         <file path="tool-data/bowtie2_indices.loc" />
    </table>
   <!-- Locations of bwa_index_color files under genome directory -->
    <table name="bwa_index_color" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/bwa_index_color.loc" />
    </table>
    <!-- Locations of codingSnps files under genome directory -->
    <table name="codingSnps" comment_char="#">
        <columns>value, path</columns>
        <file path="tool-data/codingSnps.loc" />
    </table>
    <!-- Locations of encode_datasets files under genome directory -->
    <table name="encode_datasets" comment_char="#">
        <columns>description, value, name, text,  path, format</columns>
        <file path="tool-data/encode_datasets.loc" />
    </table>
    <!-- Locations of faseq files under genome directory -->
    <table name="faseq" comment_char="#">
        <columns>value, path</columns>
        <file path="tool-data/faseq.loc" />
    </table>
 <!-- Locations of funDo files under genome directory -->
    <table name="funDo" comment_char="#">
       <columns>value, name, path</columns>
       <file path="tool-data/funDo.loc" />
    </table>
     <!-- Locations of gd.chrlens files under genome directory -->
    <table name="gd.chrlens" comment_char="#">
       <columns>value, path</columns>
       <file path="tool-data/gd.chrlens.loc" />
    </table>
    <!-- Locations of gd.heterochromatic files under genome directory -->
    <table name="gd.heterochromatic" comment_char="#">
        <columns>value, path</columns>
         <file path="tool-data/gd.heterochromatic.loc" />
     </table>
    <!-- Locations of gd.oscar files under genome directory -->
    <table name="gd.oscar" comment_char="#">
        <columns>value,  path</columns>
        <file path="tool-data/gd.oscar.loc" />
     </table>
    <!-- Locations of gd.primers files under genome directory -->
    <table name="gd.primers" comment_char="#">
        <columns>value, path</columns>
        <file path="tool-data/gd.primers.loc" />
     </table>
    <!-- Locations of gd.rank files under genome directory -->
    <table name="gd.rank" comment_char="#">
        <columns>value, prefix, kxml_dir_path, path_to_dict_file</columns>
        <file path="tool-data/gd.rank.loc" />
    </table>
    <!-- Locations of gd.snps files under genome directory -->
    <table name="gd.snps" comment_char="#">
       <columns>value, path</columns>
       <file path="tool-data/gd.snps.loc" />
   </table>
   <!-- Locations of liftOver files under genome directory -->
    <table name="liftOver" comment_char="#">
       <columns>value, dbkey, name, path</columns>
       <file path="tool-data/liftOver.loc" />
     </table>
    <!-- Locations of maf_pairwise files under genome directory -->
<table name="maf_pairwise" comment_char="#">
       <columns>value, dbkey, name, path</columns>
       <file path="tool-data/maf_pairwise.loc" />
    </table>

    <!-- Locations of quality_scores files under genome directory -->
    <table name="quality_scores" comment_char="#">
       <columns>value, dbkey, name, path</columns>
       <file path="tool-data/quality_scores.loc" />
    </table>

    <!-- Locations of regions files under genome directory -->
    <table name="regions" comment_char="#">
       <columns>value, dbkey, name, path</columns>
       <file path="tool-data/regions.loc" />
    </table>
    <!-- Locations of sam_fa_new_indices files under genome directory -->
    <table name="sam_fa_new_indices" comment_char="#">
       <columns>value, dbkey, name, path</columns>
       <file path="tool-data/sam_fa_new_indices.loc" />
    </table>
   <table name="sequence_index_base" comment_char="#">
   <!-- Locations of sequence_index_base files under genome directory -->
       <columns>value, dbkey, name, path</columns>
   </table>
    <!-- Locations of sequence_index_color files under genome directory -->
    <table name="sequence_index_color" comment_char="#">
       <columns>value, dbkey, name, path</columns>
       <file path="tool-data/sequence_index_color.loc" />
    </table>
        <!-- Locations of sift_db files under genome directory -->
    <table name="sift_db" comment_char="#">
       <columns>value, path</columns>
       <file path="tool-data/sift_db.loc" />
     </table>
    <!-- Locations of tmap_index files under genome directory -->
    <table name="tmap_index" comment_char="#">
       <columns>value, dbkey, name, path</columns>
       <file path="tool-data/tmap_index.loc" />
    </table>
    <!-- Locations of twobit files under genome directory -->
 <table name="twobit" comment_char="#">
       <columns>value,  path</columns>
       <file path="tool-data/twobit.loc" />
     </table>
    <!-- Locations of deeptools files under genome directory -->
    <table name="deepTools_seqs" comment_char="#">
       <columns>value,  name, path</columns>
        <file path="tool-data/deepTools_seqs.loc" />
    </table>
    <!-- Locations of fasta_index files under genome directory -->
    <table name="fasta_indexes" comment_char="#">
       <columns>value, dbkey, name, path</columns>
       <file path="tool-data/fasta_indexes.loc" />
    </table>
<!-- Locations of snpeff_databases files under genome directory -->
    <table name="snpeff_databases" comment_char="#">
        <columns>value,  name</columns>
        <file path="tool-data/snpeff_databases.loc" />
    </table>
    <!-- Locations of snpeff_genomedb files under genome directory -->
    <table name="snpeff_genomedb" comment_char="#">
       <columns>value, name, path</columns>
       <file path="tool-data/snpeff_genomedb.loc" />
     </table>
<!-- Locations of snpeff_regulationdb files under genome directory -->
    <table name="snpeff_regulationdb" comment_char="#">
        <columns>genome, value, name</columns>
        <file path="tool-data/snpeff_regulationdb.loc" />
    </table>
    <!-- Locations of snpeff_annotations files under genome directory -->
    <table name="snpeff_annotations" comment_char="#">
        <columns>genome, value, name</columns>
         <file path="tool-data/npeff_annotations.loc" />
     </table>
    <!-- Locations of indexes in the BWA mapper format -->
    <table name="bwa_mem_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tool-data/bwa_mem_index.loc" />
    </table>
</tables>
